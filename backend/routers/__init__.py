"""HTTP routers mounted in ``main`` with prefix ``/api`` (except static ``/``).

Each module defines a ``router`` object: ``auth``, ``projects`` (includes test-case CRUD routes),
``features``, ``generate``, ``parsers_meta``, ``export``, ``extract``, ``settings_keys``.
"""
